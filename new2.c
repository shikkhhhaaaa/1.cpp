#include <stdio.h>
#include<math.h>
int main()
{
    int n,k,s;

    printf("Enter the no: ");
    scanf("%d", &n);
    n=k;
	while(n!=0)
	{
		s=n%10;
		n=n/10;
		
	}
	if(k==s)
	printf("no is palindrome");
	else
	printf("no it is not palindrome");
}
